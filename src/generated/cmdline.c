/*
  File autogenerated by gengetopt version 2.20
  generated with the following command:
  /usr/local/bin/gengetopt --unamed-opts --input=src/generated_src/phc.ggo --file-name=src/generated/cmdline 

  The developers of gengetopt consider the fixed text that goes in all
  gengetopt output files to be in the public domain:
  we make no copyright claims on it.
*/

/* If we use autoconf.  */
#ifdef HAVE_CONFIG_H
#include "config.h"
#endif

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "getopt.h"

#include "src/generated/cmdline.h"

const char *gengetopt_args_info_purpose = "";

const char *gengetopt_args_info_usage = "Usage: " CMDLINE_PARSER_PACKAGE " [OPTIONS]... [FILES]...";

const char *gengetopt_args_info_description = "";

const char *gengetopt_args_info_help[] = {
  "  -h, --help               Print help and exit",
  "      --full-help          Print help, including hidden options, and exit",
  "  -V, --version            Print version and exit",
  "\nGENERAL OPTIONS:",
  "  -v, --verbose            Verbose output  (default=off)",
  "  -c, --compile            Compile  (default=off)",
  "      --pretty-print       Pretty print input according to the Zend style \n                             guidelines  (default=off)",
  "      --obfuscate          Obfuscate input  (default=off)",
  "      --run=STRING         Run the specified plugin (may be specified multiple \n                             times)",
  "      --r-option=STRING    Pass option to a plugin (specify multiple flags in \n                             the same order as multiple plugins - 1 option only \n                             per plugin)",
  "\nINPUT OPTIONS:",
  "      --read-ast-xml       Assume the input is a phc AST in XML format  \n                             (default=off)",
  "\nCOMPILATION OPTIONS:",
  "  -C, --c-option=STRING    Pass option to the C compile (e.g., -C-g; can be \n                             specified multiple times)",
  "      --extension=NAME     Generate a PHP extension called NAME instead of a \n                             standalone application",
  "\nPRETTY PRINTING OPTIONS:",
  "      --next-line-curlies  Output the opening curly on the next line instead of \n                             on the same line  (default=off)",
  "      --tab=STRING         String to use for tabs while unparsing  \n                             (default=`\t')",
    0
};
const char *gengetopt_args_info_full_help[] = {
  "  -h, --help               Print help and exit",
  "      --full-help          Print help, including hidden options, and exit",
  "  -V, --version            Print version and exit",
  "\nGENERAL OPTIONS:",
  "  -v, --verbose            Verbose output  (default=off)",
  "  -c, --compile            Compile  (default=off)",
  "      --pretty-print       Pretty print input according to the Zend style \n                             guidelines  (default=off)",
  "      --obfuscate          Obfuscate input  (default=off)",
  "      --run=STRING         Run the specified plugin (may be specified multiple \n                             times)",
  "      --r-option=STRING    Pass option to a plugin (specify multiple flags in \n                             the same order as multiple plugins - 1 option only \n                             per plugin)",
  "\nINPUT OPTIONS:",
  "      --read-ast-xml       Assume the input is a phc AST in XML format  \n                             (default=off)",
  "      --no-validation      Toggle XML validation  (default=on)",
  "\nCOMPILATION OPTIONS:",
  "  -C, --c-option=STRING    Pass option to the C compile (e.g., -C-g; can be \n                             specified multiple times)",
  "      --generate-c         Generate C code  (default=off)",
  "      --extension=NAME     Generate a PHP extension called NAME instead of a \n                             standalone application",
  "      --with-php=NAME      PHP installation path",
  "\nPRETTY PRINTING OPTIONS:",
  "      --next-line-curlies  Output the opening curly on the next line instead of \n                             on the same line  (default=off)",
  "      --no-line-numbers    Don't show line numbers when dumping DOT/XML  \n                             (default=off)",
  "      --no-nulls           Don't show NULLs when dumping DOT  (default=off)",
  "      --no-empty-lists     Don't show empty lists when dumping DOT  \n                             (default=off)",
  "      --tab=STRING         String to use for tabs while unparsing  \n                             (default=`\t')",
  "\nDEBUGGING PHC:",
  "  -D, --dump=passname      Dump input as PHP (although potentially with gotos \n                             and labels) after the pass named 'passname'",
  "  -U, --udump=passname     Dump input as runnable PHP after the pass named \n                             'passname'",
  "      --ddump=passname     Dump input as DOT after the pass named 'passname'",
  "  -X, --xdump=passname     Dump input as XML after the pass named 'passname'",
  "      --dont-fail          Dont fail on error (after parsing)  (default=off)",
    0
};

static
void clear_given (struct gengetopt_args_info *args_info);
static
void clear_args (struct gengetopt_args_info *args_info);

static int
cmdline_parser_internal (int argc, char * const *argv, struct gengetopt_args_info *args_info, int override, int initialize, int check_required, const char *additional_error);

static int
cmdline_parser_required2 (struct gengetopt_args_info *args_info, const char *prog_name, const char *additional_error);

struct string_list
{
  char * arg;
  char *orig;
  struct string_list * next;
};

static char *
gengetopt_strdup (const char *s);

static
void clear_given (struct gengetopt_args_info *args_info)
{
  args_info->help_given = 0 ;
  args_info->full_help_given = 0 ;
  args_info->version_given = 0 ;
  args_info->verbose_given = 0 ;
  args_info->compile_given = 0 ;
  args_info->pretty_print_given = 0 ;
  args_info->obfuscate_given = 0 ;
  args_info->run_given = 0 ;
  args_info->r_option_given = 0 ;
  args_info->read_ast_xml_given = 0 ;
  args_info->no_validation_given = 0 ;
  args_info->c_option_given = 0 ;
  args_info->generate_c_given = 0 ;
  args_info->extension_given = 0 ;
  args_info->with_php_given = 0 ;
  args_info->next_line_curlies_given = 0 ;
  args_info->no_line_numbers_given = 0 ;
  args_info->no_nulls_given = 0 ;
  args_info->no_empty_lists_given = 0 ;
  args_info->tab_given = 0 ;
  args_info->dump_given = 0 ;
  args_info->udump_given = 0 ;
  args_info->ddump_given = 0 ;
  args_info->xdump_given = 0 ;
  args_info->dont_fail_given = 0 ;
}

static
void clear_args (struct gengetopt_args_info *args_info)
{
  args_info->verbose_flag = 0;
  args_info->compile_flag = 0;
  args_info->pretty_print_flag = 0;
  args_info->obfuscate_flag = 0;
  args_info->run_arg = NULL;
  args_info->run_orig = NULL;
  args_info->r_option_arg = NULL;
  args_info->r_option_orig = NULL;
  args_info->read_ast_xml_flag = 0;
  args_info->no_validation_flag = 1;
  args_info->c_option_arg = NULL;
  args_info->c_option_orig = NULL;
  args_info->generate_c_flag = 0;
  args_info->extension_arg = NULL;
  args_info->extension_orig = NULL;
  args_info->with_php_arg = NULL;
  args_info->with_php_orig = NULL;
  args_info->next_line_curlies_flag = 0;
  args_info->no_line_numbers_flag = 0;
  args_info->no_nulls_flag = 0;
  args_info->no_empty_lists_flag = 0;
  args_info->tab_arg = gengetopt_strdup ("\t");
  args_info->tab_orig = NULL;
  args_info->dump_arg = NULL;
  args_info->dump_orig = NULL;
  args_info->udump_arg = NULL;
  args_info->udump_orig = NULL;
  args_info->ddump_arg = NULL;
  args_info->ddump_orig = NULL;
  args_info->xdump_arg = NULL;
  args_info->xdump_orig = NULL;
  args_info->dont_fail_flag = 0;
  
}

static
void init_args_info(struct gengetopt_args_info *args_info)
{
  args_info->help_help = gengetopt_args_info_full_help[0] ;
  args_info->full_help_help = gengetopt_args_info_full_help[1] ;
  args_info->version_help = gengetopt_args_info_full_help[2] ;
  args_info->verbose_help = gengetopt_args_info_full_help[4] ;
  args_info->compile_help = gengetopt_args_info_full_help[5] ;
  args_info->pretty_print_help = gengetopt_args_info_full_help[6] ;
  args_info->obfuscate_help = gengetopt_args_info_full_help[7] ;
  args_info->run_help = gengetopt_args_info_full_help[8] ;
  args_info->run_min = -1;
  args_info->run_max = -1;
  args_info->r_option_help = gengetopt_args_info_full_help[9] ;
  args_info->r_option_min = -1;
  args_info->r_option_max = -1;
  args_info->read_ast_xml_help = gengetopt_args_info_full_help[11] ;
  args_info->no_validation_help = gengetopt_args_info_full_help[12] ;
  args_info->c_option_help = gengetopt_args_info_full_help[14] ;
  args_info->c_option_min = -1;
  args_info->c_option_max = -1;
  args_info->generate_c_help = gengetopt_args_info_full_help[15] ;
  args_info->extension_help = gengetopt_args_info_full_help[16] ;
  args_info->with_php_help = gengetopt_args_info_full_help[17] ;
  args_info->next_line_curlies_help = gengetopt_args_info_full_help[19] ;
  args_info->no_line_numbers_help = gengetopt_args_info_full_help[20] ;
  args_info->no_nulls_help = gengetopt_args_info_full_help[21] ;
  args_info->no_empty_lists_help = gengetopt_args_info_full_help[22] ;
  args_info->tab_help = gengetopt_args_info_full_help[23] ;
  args_info->dump_help = gengetopt_args_info_full_help[25] ;
  args_info->dump_min = -1;
  args_info->dump_max = -1;
  args_info->udump_help = gengetopt_args_info_full_help[26] ;
  args_info->udump_min = -1;
  args_info->udump_max = -1;
  args_info->ddump_help = gengetopt_args_info_full_help[27] ;
  args_info->ddump_min = -1;
  args_info->ddump_max = -1;
  args_info->xdump_help = gengetopt_args_info_full_help[28] ;
  args_info->xdump_min = -1;
  args_info->xdump_max = -1;
  args_info->dont_fail_help = gengetopt_args_info_full_help[29] ;
  
}

void
cmdline_parser_print_version (void)
{
  printf ("%s %s\n", CMDLINE_PARSER_PACKAGE, CMDLINE_PARSER_VERSION);
}

void
cmdline_parser_print_help (void)
{
  int i = 0;
  cmdline_parser_print_version ();

  if (strlen(gengetopt_args_info_purpose) > 0)
    printf("\n%s\n", gengetopt_args_info_purpose);

  printf("\n%s\n\n", gengetopt_args_info_usage);

  if (strlen(gengetopt_args_info_description) > 0)
    printf("%s\n", gengetopt_args_info_description);

  while (gengetopt_args_info_help[i])
    printf("%s\n", gengetopt_args_info_help[i++]);
}

void
cmdline_parser_print_full_help (void)
{
  int i = 0;
  cmdline_parser_print_version ();

  if (strlen(gengetopt_args_info_purpose) > 0)
    printf("\n%s\n", gengetopt_args_info_purpose);

  printf("\n%s\n\n", gengetopt_args_info_usage);

  if (strlen(gengetopt_args_info_description) > 0)
    printf("%s\n", gengetopt_args_info_description);

  while (gengetopt_args_info_full_help[i])
    printf("%s\n", gengetopt_args_info_full_help[i++]);
}

void
cmdline_parser_init (struct gengetopt_args_info *args_info)
{
  clear_given (args_info);
  clear_args (args_info);
  init_args_info (args_info);

  args_info->inputs = NULL;
  args_info->inputs_num = 0;
}

static void
cmdline_parser_release (struct gengetopt_args_info *args_info)
{
  
  unsigned int i;
  if (args_info->run_arg)
    {
      for (i = 0; i < args_info->run_given; ++i)
        {
          if (args_info->run_arg [i])
            {
              free (args_info->run_arg [i]); /* free previous argument */
              args_info->run_arg [i] = 0;
            }
          if (args_info->run_orig [i])
            {
              free (args_info->run_orig [i]); /* free previous argument */
              args_info->run_orig [i] = 0;
            }
        }
      if (args_info->run_arg [0])
        free (args_info->run_arg [0]); /* free default string */
      free (args_info->run_arg); /* free previous argument */
      args_info->run_arg = 0;
      free (args_info->run_orig); /* free previous argument */
      args_info->run_orig = 0;
    }
  if (args_info->r_option_arg)
    {
      for (i = 0; i < args_info->r_option_given; ++i)
        {
          if (args_info->r_option_arg [i])
            {
              free (args_info->r_option_arg [i]); /* free previous argument */
              args_info->r_option_arg [i] = 0;
            }
          if (args_info->r_option_orig [i])
            {
              free (args_info->r_option_orig [i]); /* free previous argument */
              args_info->r_option_orig [i] = 0;
            }
        }
      if (args_info->r_option_arg [0])
        free (args_info->r_option_arg [0]); /* free default string */
      free (args_info->r_option_arg); /* free previous argument */
      args_info->r_option_arg = 0;
      free (args_info->r_option_orig); /* free previous argument */
      args_info->r_option_orig = 0;
    }
  if (args_info->c_option_arg)
    {
      for (i = 0; i < args_info->c_option_given; ++i)
        {
          if (args_info->c_option_arg [i])
            {
              free (args_info->c_option_arg [i]); /* free previous argument */
              args_info->c_option_arg [i] = 0;
            }
          if (args_info->c_option_orig [i])
            {
              free (args_info->c_option_orig [i]); /* free previous argument */
              args_info->c_option_orig [i] = 0;
            }
        }
      if (args_info->c_option_arg [0])
        free (args_info->c_option_arg [0]); /* free default string */
      free (args_info->c_option_arg); /* free previous argument */
      args_info->c_option_arg = 0;
      free (args_info->c_option_orig); /* free previous argument */
      args_info->c_option_orig = 0;
    }
  if (args_info->extension_arg)
    {
      free (args_info->extension_arg); /* free previous argument */
      args_info->extension_arg = 0;
    }
  if (args_info->extension_orig)
    {
      free (args_info->extension_orig); /* free previous argument */
      args_info->extension_orig = 0;
    }
  if (args_info->with_php_arg)
    {
      free (args_info->with_php_arg); /* free previous argument */
      args_info->with_php_arg = 0;
    }
  if (args_info->with_php_orig)
    {
      free (args_info->with_php_orig); /* free previous argument */
      args_info->with_php_orig = 0;
    }
  if (args_info->tab_arg)
    {
      free (args_info->tab_arg); /* free previous argument */
      args_info->tab_arg = 0;
    }
  if (args_info->tab_orig)
    {
      free (args_info->tab_orig); /* free previous argument */
      args_info->tab_orig = 0;
    }
  if (args_info->dump_arg)
    {
      for (i = 0; i < args_info->dump_given; ++i)
        {
          if (args_info->dump_arg [i])
            {
              free (args_info->dump_arg [i]); /* free previous argument */
              args_info->dump_arg [i] = 0;
            }
          if (args_info->dump_orig [i])
            {
              free (args_info->dump_orig [i]); /* free previous argument */
              args_info->dump_orig [i] = 0;
            }
        }
      if (args_info->dump_arg [0])
        free (args_info->dump_arg [0]); /* free default string */
      free (args_info->dump_arg); /* free previous argument */
      args_info->dump_arg = 0;
      free (args_info->dump_orig); /* free previous argument */
      args_info->dump_orig = 0;
    }
  if (args_info->udump_arg)
    {
      for (i = 0; i < args_info->udump_given; ++i)
        {
          if (args_info->udump_arg [i])
            {
              free (args_info->udump_arg [i]); /* free previous argument */
              args_info->udump_arg [i] = 0;
            }
          if (args_info->udump_orig [i])
            {
              free (args_info->udump_orig [i]); /* free previous argument */
              args_info->udump_orig [i] = 0;
            }
        }
      if (args_info->udump_arg [0])
        free (args_info->udump_arg [0]); /* free default string */
      free (args_info->udump_arg); /* free previous argument */
      args_info->udump_arg = 0;
      free (args_info->udump_orig); /* free previous argument */
      args_info->udump_orig = 0;
    }
  if (args_info->ddump_arg)
    {
      for (i = 0; i < args_info->ddump_given; ++i)
        {
          if (args_info->ddump_arg [i])
            {
              free (args_info->ddump_arg [i]); /* free previous argument */
              args_info->ddump_arg [i] = 0;
            }
          if (args_info->ddump_orig [i])
            {
              free (args_info->ddump_orig [i]); /* free previous argument */
              args_info->ddump_orig [i] = 0;
            }
        }
      if (args_info->ddump_arg [0])
        free (args_info->ddump_arg [0]); /* free default string */
      free (args_info->ddump_arg); /* free previous argument */
      args_info->ddump_arg = 0;
      free (args_info->ddump_orig); /* free previous argument */
      args_info->ddump_orig = 0;
    }
  if (args_info->xdump_arg)
    {
      for (i = 0; i < args_info->xdump_given; ++i)
        {
          if (args_info->xdump_arg [i])
            {
              free (args_info->xdump_arg [i]); /* free previous argument */
              args_info->xdump_arg [i] = 0;
            }
          if (args_info->xdump_orig [i])
            {
              free (args_info->xdump_orig [i]); /* free previous argument */
              args_info->xdump_orig [i] = 0;
            }
        }
      if (args_info->xdump_arg [0])
        free (args_info->xdump_arg [0]); /* free default string */
      free (args_info->xdump_arg); /* free previous argument */
      args_info->xdump_arg = 0;
      free (args_info->xdump_orig); /* free previous argument */
      args_info->xdump_orig = 0;
    }
  
  for (i = 0; i < args_info->inputs_num; ++i)
    free (args_info->inputs [i]);
  
  if (args_info->inputs_num)
    free (args_info->inputs);
  
  clear_given (args_info);
}

int
cmdline_parser_file_save(const char *filename, struct gengetopt_args_info *args_info)
{
  FILE *outfile;
  int i = 0;

  outfile = fopen(filename, "w");

  if (!outfile)
    {
      fprintf (stderr, "%s: cannot open file for writing: %s\n", CMDLINE_PARSER_PACKAGE, filename);
      return EXIT_FAILURE;
    }

  if (args_info->help_given) {
    fprintf(outfile, "%s\n", "help");
  }
  if (args_info->full_help_given) {
    fprintf(outfile, "%s\n", "full-help");
  }
  if (args_info->version_given) {
    fprintf(outfile, "%s\n", "version");
  }
  if (args_info->verbose_given) {
    fprintf(outfile, "%s\n", "verbose");
  }
  if (args_info->compile_given) {
    fprintf(outfile, "%s\n", "compile");
  }
  if (args_info->pretty_print_given) {
    fprintf(outfile, "%s\n", "pretty-print");
  }
  if (args_info->obfuscate_given) {
    fprintf(outfile, "%s\n", "obfuscate");
  }
  if (args_info->run_orig)
    {
      for (i = 0; i < args_info->run_given; ++i)
        {
          if (args_info->run_orig [i])
            {
              fprintf(outfile, "%s=\"%s\"\n", "run", args_info->run_orig [i]);
            }
        }
    }
  if (args_info->r_option_orig)
    {
      for (i = 0; i < args_info->r_option_given; ++i)
        {
          if (args_info->r_option_orig [i])
            {
              fprintf(outfile, "%s=\"%s\"\n", "r-option", args_info->r_option_orig [i]);
            }
        }
    }
  if (args_info->read_ast_xml_given) {
    fprintf(outfile, "%s\n", "read-ast-xml");
  }
  if (args_info->no_validation_given) {
    fprintf(outfile, "%s\n", "no-validation");
  }
  if (args_info->c_option_orig)
    {
      for (i = 0; i < args_info->c_option_given; ++i)
        {
          if (args_info->c_option_orig [i])
            {
              fprintf(outfile, "%s=\"%s\"\n", "c-option", args_info->c_option_orig [i]);
            }
        }
    }
  if (args_info->generate_c_given) {
    fprintf(outfile, "%s\n", "generate-c");
  }
  if (args_info->extension_given) {
    if (args_info->extension_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "extension", args_info->extension_orig);
    } else {
      fprintf(outfile, "%s\n", "extension");
    }
  }
  if (args_info->with_php_given) {
    if (args_info->with_php_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "with-php", args_info->with_php_orig);
    } else {
      fprintf(outfile, "%s\n", "with-php");
    }
  }
  if (args_info->next_line_curlies_given) {
    fprintf(outfile, "%s\n", "next-line-curlies");
  }
  if (args_info->no_line_numbers_given) {
    fprintf(outfile, "%s\n", "no-line-numbers");
  }
  if (args_info->no_nulls_given) {
    fprintf(outfile, "%s\n", "no-nulls");
  }
  if (args_info->no_empty_lists_given) {
    fprintf(outfile, "%s\n", "no-empty-lists");
  }
  if (args_info->tab_given) {
    if (args_info->tab_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "tab", args_info->tab_orig);
    } else {
      fprintf(outfile, "%s\n", "tab");
    }
  }
  if (args_info->dump_orig)
    {
      for (i = 0; i < args_info->dump_given; ++i)
        {
          if (args_info->dump_orig [i])
            {
              fprintf(outfile, "%s=\"%s\"\n", "dump", args_info->dump_orig [i]);
            }
        }
    }
  if (args_info->udump_orig)
    {
      for (i = 0; i < args_info->udump_given; ++i)
        {
          if (args_info->udump_orig [i])
            {
              fprintf(outfile, "%s=\"%s\"\n", "udump", args_info->udump_orig [i]);
            }
        }
    }
  if (args_info->ddump_orig)
    {
      for (i = 0; i < args_info->ddump_given; ++i)
        {
          if (args_info->ddump_orig [i])
            {
              fprintf(outfile, "%s=\"%s\"\n", "ddump", args_info->ddump_orig [i]);
            }
        }
    }
  if (args_info->xdump_orig)
    {
      for (i = 0; i < args_info->xdump_given; ++i)
        {
          if (args_info->xdump_orig [i])
            {
              fprintf(outfile, "%s=\"%s\"\n", "xdump", args_info->xdump_orig [i]);
            }
        }
    }
  if (args_info->dont_fail_given) {
    fprintf(outfile, "%s\n", "dont-fail");
  }
  
  fclose (outfile);

  i = EXIT_SUCCESS;
  return i;
}

void
cmdline_parser_free (struct gengetopt_args_info *args_info)
{
  cmdline_parser_release (args_info);
}


/* gengetopt_strdup() */
/* strdup.c replacement of strdup, which is not standard */
char *
gengetopt_strdup (const char *s)
{
  char *result = NULL;
  if (!s)
    return result;

  result = (char*)malloc(strlen(s) + 1);
  if (result == (char*)0)
    return (char*)0;
  strcpy(result, s);
  return result;
}

static char *
get_multiple_arg_token(const char *arg)
{
  char *tok, *ret;
  size_t len, num_of_escape;
  int i, j;

  if (!arg)
    return NULL;

  tok = strchr (arg, ',');
  num_of_escape = 0;

  /* make sure it is not escaped */
  while (tok)
    {
      if (*(tok-1) == '\\')
        {
          /* find the next one */
          tok = strchr (tok+1, ',');
          ++num_of_escape;
        }
      else
        break;
    }

  if (tok)
    len = (size_t)(tok - arg + 1);
  else
    len = strlen (arg) + 1;

  len -= num_of_escape;

  ret = (char *) malloc (len);

  i = 0;
  j = 0;
  while (arg[i] && (j < len-1))
    {
      if (arg[i] == '\\')
        ++i;

      ret[j++] = arg[i++];
    }

  ret[len-1] = '\0';

  return ret;
}

static char *
get_multiple_arg_token_next(const char *arg)
{
  char *tok;

  if (!arg)
    return NULL;

  tok = strchr (arg, ',');

  /* make sure it is not escaped */
  while (tok)
    {
      if (*(tok-1) == '\\')
        {
          /* find the next one */
          tok = strchr (tok+1, ',');
        }
      else
        break;
    }

  if (! tok || strlen(tok) == 1)
    return 0;

  return tok+1;
}

static int
check_multiple_option_occurrences(const char *prog_name, unsigned int option_given, int min, int max, const char *option_desc);

int
check_multiple_option_occurrences(const char *prog_name, unsigned int option_given, int min, int max, const char *option_desc)
{
  int error = 0;

  if (option_given && ! (min < 0 && max < 0))
    {
      if (min >= 0 && max >= 0)
        {
          if (min == max)
            {
              /* specific occurrences */
              if (option_given != min)
                {
                  fprintf (stderr, "%s: %s option occurrences must be %d\n",
                    prog_name, option_desc, min);
                  error = 1;
                }
            }
          else if (option_given < min
              || option_given > max)
            {
              /* range occurrences */
              fprintf (stderr, "%s: %s option occurrences must be between %d and %d\n",
                prog_name, option_desc, min, max);
              error = 1;
            }
        }
      else if (min >= 0)
        {
          /* at least check */
          if (option_given < min)
            {
              fprintf (stderr, "%s: %s option occurrences must be at least %d\n",
                prog_name, option_desc, min);
              error = 1;
            }
        }
      else if (max >= 0)
        {
          /* at most check */
          if (option_given > max)
            {
              fprintf (stderr, "%s: %s option occurrences must be at most %d\n",
                prog_name, option_desc, max);
              error = 1;
            }
        }
    }
    
  return error;
}
int
cmdline_parser (int argc, char * const *argv, struct gengetopt_args_info *args_info)
{
  return cmdline_parser2 (argc, argv, args_info, 0, 1, 1);
}

int
cmdline_parser2 (int argc, char * const *argv, struct gengetopt_args_info *args_info, int override, int initialize, int check_required)
{
  int result;

  result = cmdline_parser_internal (argc, argv, args_info, override, initialize, check_required, NULL);

  if (result == EXIT_FAILURE)
    {
      cmdline_parser_free (args_info);
      exit (EXIT_FAILURE);
    }
  
  return result;
}

int
cmdline_parser_required (struct gengetopt_args_info *args_info, const char *prog_name)
{
  int result = EXIT_SUCCESS;

  if (cmdline_parser_required2(args_info, prog_name, NULL) > 0)
    result = EXIT_FAILURE;

  if (result == EXIT_FAILURE)
    {
      cmdline_parser_free (args_info);
      exit (EXIT_FAILURE);
    }
  
  return result;
}

int
cmdline_parser_required2 (struct gengetopt_args_info *args_info, const char *prog_name, const char *additional_error)
{
  int error = 0;

  /* checks for required options */
  if (check_multiple_option_occurrences(prog_name, args_info->run_given, args_info->run_min, args_info->run_max, "'--run'"))
     error = 1;
  
  if (check_multiple_option_occurrences(prog_name, args_info->r_option_given, args_info->r_option_min, args_info->r_option_max, "'--r-option'"))
     error = 1;
  
  if (check_multiple_option_occurrences(prog_name, args_info->c_option_given, args_info->c_option_min, args_info->c_option_max, "'--c-option' ('-C')"))
     error = 1;
  
  if (check_multiple_option_occurrences(prog_name, args_info->dump_given, args_info->dump_min, args_info->dump_max, "'--dump' ('-D')"))
     error = 1;
  
  if (check_multiple_option_occurrences(prog_name, args_info->udump_given, args_info->udump_min, args_info->udump_max, "'--udump' ('-U')"))
     error = 1;
  
  if (check_multiple_option_occurrences(prog_name, args_info->ddump_given, args_info->ddump_min, args_info->ddump_max, "'--ddump'"))
     error = 1;
  
  if (check_multiple_option_occurrences(prog_name, args_info->xdump_given, args_info->xdump_min, args_info->xdump_max, "'--xdump' ('-X')"))
     error = 1;
  
  
  /* checks for dependences among options */

  return error;
}

int
cmdline_parser_internal (int argc, char * const *argv, struct gengetopt_args_info *args_info, int override, int initialize, int check_required, const char *additional_error)
{
  int c;	/* Character of the parsed option.  */
  char *multi_token, *multi_next; /* for multiple options */

  int i;        /* Counter */

  struct string_list * run_list = NULL,* run_new = NULL;
  struct string_list * r_option_list = NULL,* r_option_new = NULL;
  struct string_list * c_option_list = NULL,* c_option_new = NULL;
  struct string_list * dump_list = NULL,* dump_new = NULL;
  struct string_list * udump_list = NULL,* udump_new = NULL;
  struct string_list * ddump_list = NULL,* ddump_new = NULL;
  struct string_list * xdump_list = NULL,* xdump_new = NULL;
  int error = 0;
  struct gengetopt_args_info local_args_info;

  if (initialize)
    cmdline_parser_init (args_info);

  cmdline_parser_init (&local_args_info);

  optarg = 0;
  optind = 0;
  opterr = 1;
  optopt = '?';

  while (1)
    {
      int option_index = 0;
      char *stop_char;

      static struct option long_options[] = {
        { "help",	0, NULL, 'h' },
        { "full-help",	0, NULL, 0 },
        { "version",	0, NULL, 'V' },
        { "verbose",	0, NULL, 'v' },
        { "compile",	0, NULL, 'c' },
        { "pretty-print",	0, NULL, 0 },
        { "obfuscate",	0, NULL, 0 },
        { "run",	1, NULL, 0 },
        { "r-option",	1, NULL, 0 },
        { "read-ast-xml",	0, NULL, 0 },
        { "no-validation",	0, NULL, 0 },
        { "c-option",	1, NULL, 'C' },
        { "generate-c",	0, NULL, 0 },
        { "extension",	1, NULL, 0 },
        { "with-php",	1, NULL, 0 },
        { "next-line-curlies",	0, NULL, 0 },
        { "no-line-numbers",	0, NULL, 0 },
        { "no-nulls",	0, NULL, 0 },
        { "no-empty-lists",	0, NULL, 0 },
        { "tab",	1, NULL, 0 },
        { "dump",	1, NULL, 'D' },
        { "udump",	1, NULL, 'U' },
        { "ddump",	1, NULL, 0 },
        { "xdump",	1, NULL, 'X' },
        { "dont-fail",	0, NULL, 0 },
        { NULL,	0, NULL, 0 }
      };

      stop_char = 0;
      c = getopt_long (argc, argv, "hVvcC:D:U:X:", long_options, &option_index);

      if (c == -1) break;	/* Exit from `while (1)' loop.  */

      switch (c)
        {
        case 'h':	/* Print help and exit.  */
          cmdline_parser_print_help ();
          cmdline_parser_free (&local_args_info);
          exit (EXIT_SUCCESS);

        case 'V':	/* Print version and exit.  */
          cmdline_parser_print_version ();
          cmdline_parser_free (&local_args_info);
          exit (EXIT_SUCCESS);

        case 'v':	/* Verbose output.  */
          if (local_args_info.verbose_given)
            {
              fprintf (stderr, "%s: `--verbose' (`-v') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->verbose_given && ! override)
            continue;
          local_args_info.verbose_given = 1;
          args_info->verbose_given = 1;
          args_info->verbose_flag = !(args_info->verbose_flag);
          break;

        case 'c':	/* Compile.  */
          if (local_args_info.compile_given)
            {
              fprintf (stderr, "%s: `--compile' (`-c') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->compile_given && ! override)
            continue;
          local_args_info.compile_given = 1;
          args_info->compile_given = 1;
          args_info->compile_flag = !(args_info->compile_flag);
          break;

        case 'C':	/* Pass option to the C compile (e.g., -C-g; can be specified multiple times).  */
          local_args_info.c_option_given++;
        
          multi_token = get_multiple_arg_token(optarg);
          multi_next = get_multiple_arg_token_next (optarg);
        
          while (1)
            {
              c_option_new = (struct string_list *) malloc (sizeof (struct string_list));
              c_option_new->next = c_option_list;
              c_option_list = c_option_new;
              c_option_new->arg = gengetopt_strdup (multi_token);
              c_option_new->orig = multi_token;
        
              if (multi_next)
                {
                  multi_token = get_multiple_arg_token(multi_next);
                  multi_next = get_multiple_arg_token_next (multi_next);
                  local_args_info.c_option_given++;
                }
              else
                break;
            }
          break;

        case 'D':	/* Dump input as PHP (although potentially with gotos and labels) after the pass named 'passname'.  */
          local_args_info.dump_given++;
        
          multi_token = get_multiple_arg_token(optarg);
          multi_next = get_multiple_arg_token_next (optarg);
        
          while (1)
            {
              dump_new = (struct string_list *) malloc (sizeof (struct string_list));
              dump_new->next = dump_list;
              dump_list = dump_new;
              dump_new->arg = gengetopt_strdup (multi_token);
              dump_new->orig = multi_token;
        
              if (multi_next)
                {
                  multi_token = get_multiple_arg_token(multi_next);
                  multi_next = get_multiple_arg_token_next (multi_next);
                  local_args_info.dump_given++;
                }
              else
                break;
            }
          break;

        case 'U':	/* Dump input as runnable PHP after the pass named 'passname'.  */
          local_args_info.udump_given++;
        
          multi_token = get_multiple_arg_token(optarg);
          multi_next = get_multiple_arg_token_next (optarg);
        
          while (1)
            {
              udump_new = (struct string_list *) malloc (sizeof (struct string_list));
              udump_new->next = udump_list;
              udump_list = udump_new;
              udump_new->arg = gengetopt_strdup (multi_token);
              udump_new->orig = multi_token;
        
              if (multi_next)
                {
                  multi_token = get_multiple_arg_token(multi_next);
                  multi_next = get_multiple_arg_token_next (multi_next);
                  local_args_info.udump_given++;
                }
              else
                break;
            }
          break;

        case 'X':	/* Dump input as XML after the pass named 'passname'.  */
          local_args_info.xdump_given++;
        
          multi_token = get_multiple_arg_token(optarg);
          multi_next = get_multiple_arg_token_next (optarg);
        
          while (1)
            {
              xdump_new = (struct string_list *) malloc (sizeof (struct string_list));
              xdump_new->next = xdump_list;
              xdump_list = xdump_new;
              xdump_new->arg = gengetopt_strdup (multi_token);
              xdump_new->orig = multi_token;
        
              if (multi_next)
                {
                  multi_token = get_multiple_arg_token(multi_next);
                  multi_next = get_multiple_arg_token_next (multi_next);
                  local_args_info.xdump_given++;
                }
              else
                break;
            }
          break;


        case 0:	/* Long option with no short option */
          if (strcmp (long_options[option_index].name, "full-help") == 0) {
            cmdline_parser_print_full_help ();
            cmdline_parser_free (&local_args_info);
            exit (EXIT_SUCCESS);
          }
          

          /* Pretty print input according to the Zend style guidelines.  */
          if (strcmp (long_options[option_index].name, "pretty-print") == 0)
          {
            if (local_args_info.pretty_print_given)
              {
                fprintf (stderr, "%s: `--pretty-print' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->pretty_print_given && ! override)
              continue;
            local_args_info.pretty_print_given = 1;
            args_info->pretty_print_given = 1;
            args_info->pretty_print_flag = !(args_info->pretty_print_flag);
          }
          /* Obfuscate input.  */
          else if (strcmp (long_options[option_index].name, "obfuscate") == 0)
          {
            if (local_args_info.obfuscate_given)
              {
                fprintf (stderr, "%s: `--obfuscate' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->obfuscate_given && ! override)
              continue;
            local_args_info.obfuscate_given = 1;
            args_info->obfuscate_given = 1;
            args_info->obfuscate_flag = !(args_info->obfuscate_flag);
          }
          /* Run the specified plugin (may be specified multiple times).  */
          else if (strcmp (long_options[option_index].name, "run") == 0)
          {
            local_args_info.run_given++;
          
            multi_token = get_multiple_arg_token(optarg);
            multi_next = get_multiple_arg_token_next (optarg);
          
            while (1)
              {
                run_new = (struct string_list *) malloc (sizeof (struct string_list));
                run_new->next = run_list;
                run_list = run_new;
                run_new->arg = gengetopt_strdup (multi_token);
                run_new->orig = multi_token;
          
                if (multi_next)
                  {
                    multi_token = get_multiple_arg_token(multi_next);
                    multi_next = get_multiple_arg_token_next (multi_next);
                    local_args_info.run_given++;
                  }
                else
                  break;
              }
            break;
          }
          /* Pass option to a plugin (specify multiple flags in the same order as multiple plugins - 1 option only per plugin).  */
          else if (strcmp (long_options[option_index].name, "r-option") == 0)
          {
            local_args_info.r_option_given++;
          
            multi_token = get_multiple_arg_token(optarg);
            multi_next = get_multiple_arg_token_next (optarg);
          
            while (1)
              {
                r_option_new = (struct string_list *) malloc (sizeof (struct string_list));
                r_option_new->next = r_option_list;
                r_option_list = r_option_new;
                r_option_new->arg = gengetopt_strdup (multi_token);
                r_option_new->orig = multi_token;
          
                if (multi_next)
                  {
                    multi_token = get_multiple_arg_token(multi_next);
                    multi_next = get_multiple_arg_token_next (multi_next);
                    local_args_info.r_option_given++;
                  }
                else
                  break;
              }
            break;
          }
          /* Assume the input is a phc AST in XML format.  */
          else if (strcmp (long_options[option_index].name, "read-ast-xml") == 0)
          {
            if (local_args_info.read_ast_xml_given)
              {
                fprintf (stderr, "%s: `--read-ast-xml' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->read_ast_xml_given && ! override)
              continue;
            local_args_info.read_ast_xml_given = 1;
            args_info->read_ast_xml_given = 1;
            args_info->read_ast_xml_flag = !(args_info->read_ast_xml_flag);
          }
          /* Toggle XML validation.  */
          else if (strcmp (long_options[option_index].name, "no-validation") == 0)
          {
            if (local_args_info.no_validation_given)
              {
                fprintf (stderr, "%s: `--no-validation' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->no_validation_given && ! override)
              continue;
            local_args_info.no_validation_given = 1;
            args_info->no_validation_given = 1;
            args_info->no_validation_flag = !(args_info->no_validation_flag);
          }
          /* Generate C code.  */
          else if (strcmp (long_options[option_index].name, "generate-c") == 0)
          {
            if (local_args_info.generate_c_given)
              {
                fprintf (stderr, "%s: `--generate-c' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->generate_c_given && ! override)
              continue;
            local_args_info.generate_c_given = 1;
            args_info->generate_c_given = 1;
            args_info->generate_c_flag = !(args_info->generate_c_flag);
          }
          /* Generate a PHP extension called NAME instead of a standalone application.  */
          else if (strcmp (long_options[option_index].name, "extension") == 0)
          {
            if (local_args_info.extension_given)
              {
                fprintf (stderr, "%s: `--extension' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->extension_given && ! override)
              continue;
            local_args_info.extension_given = 1;
            args_info->extension_given = 1;
            if (args_info->extension_arg)
              free (args_info->extension_arg); /* free previous string */
            args_info->extension_arg = gengetopt_strdup (optarg);
            if (args_info->extension_orig)
              free (args_info->extension_orig); /* free previous string */
            args_info->extension_orig = gengetopt_strdup (optarg);
          }
          /* PHP installation path.  */
          else if (strcmp (long_options[option_index].name, "with-php") == 0)
          {
            if (local_args_info.with_php_given)
              {
                fprintf (stderr, "%s: `--with-php' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->with_php_given && ! override)
              continue;
            local_args_info.with_php_given = 1;
            args_info->with_php_given = 1;
            if (args_info->with_php_arg)
              free (args_info->with_php_arg); /* free previous string */
            args_info->with_php_arg = gengetopt_strdup (optarg);
            if (args_info->with_php_orig)
              free (args_info->with_php_orig); /* free previous string */
            args_info->with_php_orig = gengetopt_strdup (optarg);
          }
          /* Output the opening curly on the next line instead of on the same line.  */
          else if (strcmp (long_options[option_index].name, "next-line-curlies") == 0)
          {
            if (local_args_info.next_line_curlies_given)
              {
                fprintf (stderr, "%s: `--next-line-curlies' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->next_line_curlies_given && ! override)
              continue;
            local_args_info.next_line_curlies_given = 1;
            args_info->next_line_curlies_given = 1;
            args_info->next_line_curlies_flag = !(args_info->next_line_curlies_flag);
          }
          /* Don't show line numbers when dumping DOT/XML.  */
          else if (strcmp (long_options[option_index].name, "no-line-numbers") == 0)
          {
            if (local_args_info.no_line_numbers_given)
              {
                fprintf (stderr, "%s: `--no-line-numbers' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->no_line_numbers_given && ! override)
              continue;
            local_args_info.no_line_numbers_given = 1;
            args_info->no_line_numbers_given = 1;
            args_info->no_line_numbers_flag = !(args_info->no_line_numbers_flag);
          }
          /* Don't show NULLs when dumping DOT.  */
          else if (strcmp (long_options[option_index].name, "no-nulls") == 0)
          {
            if (local_args_info.no_nulls_given)
              {
                fprintf (stderr, "%s: `--no-nulls' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->no_nulls_given && ! override)
              continue;
            local_args_info.no_nulls_given = 1;
            args_info->no_nulls_given = 1;
            args_info->no_nulls_flag = !(args_info->no_nulls_flag);
          }
          /* Don't show empty lists when dumping DOT.  */
          else if (strcmp (long_options[option_index].name, "no-empty-lists") == 0)
          {
            if (local_args_info.no_empty_lists_given)
              {
                fprintf (stderr, "%s: `--no-empty-lists' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->no_empty_lists_given && ! override)
              continue;
            local_args_info.no_empty_lists_given = 1;
            args_info->no_empty_lists_given = 1;
            args_info->no_empty_lists_flag = !(args_info->no_empty_lists_flag);
          }
          /* String to use for tabs while unparsing.  */
          else if (strcmp (long_options[option_index].name, "tab") == 0)
          {
            if (local_args_info.tab_given)
              {
                fprintf (stderr, "%s: `--tab' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->tab_given && ! override)
              continue;
            local_args_info.tab_given = 1;
            args_info->tab_given = 1;
            if (args_info->tab_arg)
              free (args_info->tab_arg); /* free previous string */
            args_info->tab_arg = gengetopt_strdup (optarg);
            if (args_info->tab_orig)
              free (args_info->tab_orig); /* free previous string */
            args_info->tab_orig = gengetopt_strdup (optarg);
          }
          /* Dump input as DOT after the pass named 'passname'.  */
          else if (strcmp (long_options[option_index].name, "ddump") == 0)
          {
            local_args_info.ddump_given++;
          
            multi_token = get_multiple_arg_token(optarg);
            multi_next = get_multiple_arg_token_next (optarg);
          
            while (1)
              {
                ddump_new = (struct string_list *) malloc (sizeof (struct string_list));
                ddump_new->next = ddump_list;
                ddump_list = ddump_new;
                ddump_new->arg = gengetopt_strdup (multi_token);
                ddump_new->orig = multi_token;
          
                if (multi_next)
                  {
                    multi_token = get_multiple_arg_token(multi_next);
                    multi_next = get_multiple_arg_token_next (multi_next);
                    local_args_info.ddump_given++;
                  }
                else
                  break;
              }
            break;
          }
          /* Dont fail on error (after parsing).  */
          else if (strcmp (long_options[option_index].name, "dont-fail") == 0)
          {
            if (local_args_info.dont_fail_given)
              {
                fprintf (stderr, "%s: `--dont-fail' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->dont_fail_given && ! override)
              continue;
            local_args_info.dont_fail_given = 1;
            args_info->dont_fail_given = 1;
            args_info->dont_fail_flag = !(args_info->dont_fail_flag);
          }
          
          break;
        case '?':	/* Invalid option.  */
          /* `getopt_long' already printed an error message.  */
          goto failure;

        default:	/* bug: option not considered.  */
          fprintf (stderr, "%s: option unknown: %c%s\n", CMDLINE_PARSER_PACKAGE, c, (additional_error ? additional_error : ""));
          abort ();
        } /* switch */
    } /* while */


  if (local_args_info.run_given && run_list)
    {
      struct string_list *tmp;
      args_info->run_arg = (char * *) realloc (args_info->run_arg, (args_info->run_given + local_args_info.run_given) * sizeof (char *));
      args_info->run_orig = (char **) realloc (args_info->run_orig, (args_info->run_given + local_args_info.run_given) * sizeof (char *));
      for (i = (local_args_info.run_given - 1); i >= 0; --i)
        {
          tmp = run_list;
          args_info->run_arg [i + args_info->run_given] = run_list->arg;
          args_info->run_orig [i + args_info->run_given] = run_list->orig;
          run_list = run_list->next;
          free (tmp);
        }
    }
  
  if (local_args_info.r_option_given && r_option_list)
    {
      struct string_list *tmp;
      args_info->r_option_arg = (char * *) realloc (args_info->r_option_arg, (args_info->r_option_given + local_args_info.r_option_given) * sizeof (char *));
      args_info->r_option_orig = (char **) realloc (args_info->r_option_orig, (args_info->r_option_given + local_args_info.r_option_given) * sizeof (char *));
      for (i = (local_args_info.r_option_given - 1); i >= 0; --i)
        {
          tmp = r_option_list;
          args_info->r_option_arg [i + args_info->r_option_given] = r_option_list->arg;
          args_info->r_option_orig [i + args_info->r_option_given] = r_option_list->orig;
          r_option_list = r_option_list->next;
          free (tmp);
        }
    }
  
  if (local_args_info.c_option_given && c_option_list)
    {
      struct string_list *tmp;
      args_info->c_option_arg = (char * *) realloc (args_info->c_option_arg, (args_info->c_option_given + local_args_info.c_option_given) * sizeof (char *));
      args_info->c_option_orig = (char **) realloc (args_info->c_option_orig, (args_info->c_option_given + local_args_info.c_option_given) * sizeof (char *));
      for (i = (local_args_info.c_option_given - 1); i >= 0; --i)
        {
          tmp = c_option_list;
          args_info->c_option_arg [i + args_info->c_option_given] = c_option_list->arg;
          args_info->c_option_orig [i + args_info->c_option_given] = c_option_list->orig;
          c_option_list = c_option_list->next;
          free (tmp);
        }
    }
  
  if (local_args_info.dump_given && dump_list)
    {
      struct string_list *tmp;
      args_info->dump_arg = (char * *) realloc (args_info->dump_arg, (args_info->dump_given + local_args_info.dump_given) * sizeof (char *));
      args_info->dump_orig = (char **) realloc (args_info->dump_orig, (args_info->dump_given + local_args_info.dump_given) * sizeof (char *));
      for (i = (local_args_info.dump_given - 1); i >= 0; --i)
        {
          tmp = dump_list;
          args_info->dump_arg [i + args_info->dump_given] = dump_list->arg;
          args_info->dump_orig [i + args_info->dump_given] = dump_list->orig;
          dump_list = dump_list->next;
          free (tmp);
        }
    }
  
  if (local_args_info.udump_given && udump_list)
    {
      struct string_list *tmp;
      args_info->udump_arg = (char * *) realloc (args_info->udump_arg, (args_info->udump_given + local_args_info.udump_given) * sizeof (char *));
      args_info->udump_orig = (char **) realloc (args_info->udump_orig, (args_info->udump_given + local_args_info.udump_given) * sizeof (char *));
      for (i = (local_args_info.udump_given - 1); i >= 0; --i)
        {
          tmp = udump_list;
          args_info->udump_arg [i + args_info->udump_given] = udump_list->arg;
          args_info->udump_orig [i + args_info->udump_given] = udump_list->orig;
          udump_list = udump_list->next;
          free (tmp);
        }
    }
  
  if (local_args_info.ddump_given && ddump_list)
    {
      struct string_list *tmp;
      args_info->ddump_arg = (char * *) realloc (args_info->ddump_arg, (args_info->ddump_given + local_args_info.ddump_given) * sizeof (char *));
      args_info->ddump_orig = (char **) realloc (args_info->ddump_orig, (args_info->ddump_given + local_args_info.ddump_given) * sizeof (char *));
      for (i = (local_args_info.ddump_given - 1); i >= 0; --i)
        {
          tmp = ddump_list;
          args_info->ddump_arg [i + args_info->ddump_given] = ddump_list->arg;
          args_info->ddump_orig [i + args_info->ddump_given] = ddump_list->orig;
          ddump_list = ddump_list->next;
          free (tmp);
        }
    }
  
  if (local_args_info.xdump_given && xdump_list)
    {
      struct string_list *tmp;
      args_info->xdump_arg = (char * *) realloc (args_info->xdump_arg, (args_info->xdump_given + local_args_info.xdump_given) * sizeof (char *));
      args_info->xdump_orig = (char **) realloc (args_info->xdump_orig, (args_info->xdump_given + local_args_info.xdump_given) * sizeof (char *));
      for (i = (local_args_info.xdump_given - 1); i >= 0; --i)
        {
          tmp = xdump_list;
          args_info->xdump_arg [i + args_info->xdump_given] = xdump_list->arg;
          args_info->xdump_orig [i + args_info->xdump_given] = xdump_list->orig;
          xdump_list = xdump_list->next;
          free (tmp);
        }
    }
  

  args_info->run_given += local_args_info.run_given;
  local_args_info.run_given = 0;
  args_info->r_option_given += local_args_info.r_option_given;
  local_args_info.r_option_given = 0;
  args_info->c_option_given += local_args_info.c_option_given;
  local_args_info.c_option_given = 0;
  args_info->dump_given += local_args_info.dump_given;
  local_args_info.dump_given = 0;
  args_info->udump_given += local_args_info.udump_given;
  local_args_info.udump_given = 0;
  args_info->ddump_given += local_args_info.ddump_given;
  local_args_info.ddump_given = 0;
  args_info->xdump_given += local_args_info.xdump_given;
  local_args_info.xdump_given = 0;
  
  if (check_required)
    {
      error += cmdline_parser_required2 (args_info, argv[0], additional_error);
    }

  cmdline_parser_release (&local_args_info);

  if ( error )
    return (EXIT_FAILURE);

  if (optind < argc)
    {
      int i = 0 ;
      int found_prog_name = 0;
      /* whether program name, i.e., argv[0], is in the remaining args
         (this may happen with some implementations of getopt,
          but surely not with the one included by gengetopt) */

      i = optind;
      while (i < argc)
        if (argv[i++] == argv[0]) {
          found_prog_name = 1;
          break;
        }
      i = 0;

      args_info->inputs_num = argc - optind - found_prog_name;
      args_info->inputs =
        (char **)(malloc ((args_info->inputs_num)*sizeof(char *))) ;
      while (optind < argc)
        if (argv[optind++] != argv[0])
          args_info->inputs[ i++ ] = gengetopt_strdup (argv[optind-1]) ;
    }

  return 0;

failure:
  if (run_list)
    {
      struct string_list *tmp;
      while (run_list)
        {
          tmp = run_list;
          free (run_list->arg);
          free (run_list->orig);
          run_list = run_list->next;
          free (tmp);
        }
    }
  if (r_option_list)
    {
      struct string_list *tmp;
      while (r_option_list)
        {
          tmp = r_option_list;
          free (r_option_list->arg);
          free (r_option_list->orig);
          r_option_list = r_option_list->next;
          free (tmp);
        }
    }
  if (c_option_list)
    {
      struct string_list *tmp;
      while (c_option_list)
        {
          tmp = c_option_list;
          free (c_option_list->arg);
          free (c_option_list->orig);
          c_option_list = c_option_list->next;
          free (tmp);
        }
    }
  if (dump_list)
    {
      struct string_list *tmp;
      while (dump_list)
        {
          tmp = dump_list;
          free (dump_list->arg);
          free (dump_list->orig);
          dump_list = dump_list->next;
          free (tmp);
        }
    }
  if (udump_list)
    {
      struct string_list *tmp;
      while (udump_list)
        {
          tmp = udump_list;
          free (udump_list->arg);
          free (udump_list->orig);
          udump_list = udump_list->next;
          free (tmp);
        }
    }
  if (ddump_list)
    {
      struct string_list *tmp;
      while (ddump_list)
        {
          tmp = ddump_list;
          free (ddump_list->arg);
          free (ddump_list->orig);
          ddump_list = ddump_list->next;
          free (tmp);
        }
    }
  if (xdump_list)
    {
      struct string_list *tmp;
      while (xdump_list)
        {
          tmp = xdump_list;
          free (xdump_list->arg);
          free (xdump_list->orig);
          xdump_list = xdump_list->next;
          free (tmp);
        }
    }
  
  cmdline_parser_release (&local_args_info);
  return (EXIT_FAILURE);
}
