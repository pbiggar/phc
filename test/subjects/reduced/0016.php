<?php
	// Reduced with command line 'test/framework/reduce -v test/subjects/codegen/indexing_non_arrays.php'
	
	function write($init)
	{
		$TSe20 = var_dump($init[$TLE23]);
	}
	$check[] = "a";
	$PLA0 =& each($check);
	$init = $check[$PLA0[0]];

	write ($init);
?>
