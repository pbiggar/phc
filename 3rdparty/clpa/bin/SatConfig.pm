use lib "$ENV{CILROOT}/lib"; # The libraries are in the lib directory
$::archos    = "x86_LINUX";
$::cc        = "gcc";
$::cilhome   = $ENV{CILROOT};
$::sathome   = $ENV{SATROOT};
$::default_mode = "GNUCC";
